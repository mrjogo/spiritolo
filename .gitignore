# Common OS and editor artifacts
.DS_Store
.AppleDouble
.LSOverride
*~
.directory

# Environment files — never commit secrets
.env
.env.*
!.env.example

# Log files
*.log

# Local data — HTML archive and scraper state DB
data/

# Python artifacts
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.coverage
.coverage.*
htmlcov/

# Worktrees
.worktrees/
.claude/worktrees/

# Brainstorm visual companion mockups
.superpowers/

# DevPod build scratch (regenerated per `devpod up`)
.devcontainer/.devpod-internal/

# Playwright MCP screenshot/snapshot scratch (kept local, never committed)
.playwright-mcp/
/*.png
data/batches/
